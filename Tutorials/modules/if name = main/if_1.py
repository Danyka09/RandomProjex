print(f"if_1 __name__: {__name__}")

if __name__ == '__main__':

# its useful when we import files as modules, when we directly execute the file the code will run, as a module it wont, like test code
    print('name equals main')
# name is a variable
# python automatically creates variables in the background (Dunders)
# as a script name always = main
# as a module name = modules name